# Brain index

Generated by brain.py index; do not edit. 6 active, 0 superseded. Unreviewed notes wait in _pending.md.

- [[a-session-can-launch-a-detached-run-create-no-window-survive]] A session can launch a detached run; CREATE_NO_WINDOW survives the terminal closing: Claude Code on Windows puts tool and claude -p children in no job and kills no tree, so they outlive the call and the session. Closing the terminal kills plain children; CREATE_NO_WINDOW ones live. (spike on yah/autopilot-auto, Windows 11, 2026-09-25)
- [[auto-merge-is-opt-in-yah-merges-only-green-prs-it-opened]] Auto-merge is opt-in: yah merges only green PRs it opened: Off unless /yah:setup opts in. Only the run driver merges its own phase PR, judged on a read taken after checks pass: a check ran, CLEAN, not draft, not stacked. Merge commit, retarget, then delete. (user, 2026-09-25)
- [[beads-is-a-frozen-adapter-every-plan-state-feature-ships-in]] Beads is a frozen adapter: every plan-state feature ships in STATE.md first: STATE.md is the default and wins over a beads epic. Beads may not offer anything STATE.md lacks; worktrees read the main checkout's STATE.md like .beads. (PR #6; user, 2026-09-24)
- [[skill-allowed-tools-patterns-must-allow-for-the-quoted-scrip]] Skill allowed-tools patterns must allow for the quoted script path: Skills run PY "${CLAUDE_SKILL_DIR}/../../scripts/x.py" --flag, so the path ends in a quote: write Bash(python *scripts/x.py* --flag), never *scripts/x.py --flag*, which cannot match. (skills/auto/SKILL.md:6)
- [[stacked-prs-into-main-merge-with-a-merge-commit-not-squash]] Stacked PRs into main merge with a merge commit, not squash: main takes squash merges, but a stack's base needs a merge commit or the next PR conflicts on retarget. Retarget each PR to main before deleting its base branch, or GitHub closes it. (PR #3, #6, #7 merged clean 2026-09-25; #5 closed when #4's branch was deleted)
- [[yah-run-stop-hard-kills-the-run-s-tree-then-writes-exit-130]] yah run --stop hard-kills the run's tree, then writes exit 130 itself: No graceful stop: --stop kills the driver and everything under it (taskkill /T; elsewhere SIGSTOP, then SIGKILL the tree), waits for the lock to free, then writes the pid file's exit 130. (scripts/run.py:813)
